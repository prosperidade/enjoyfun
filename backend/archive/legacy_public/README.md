# Legacy Public Scripts

Arquivos neste diretorio foram retirados de `backend/public/` porque nao fazem
parte do runtime HTTP oficial e aumentavam risco operacional ou confusao.

Conteudo atual:

- `run_migration.php`: script legado de seed/migracao manual; mantido apenas
  para rastreabilidade historica, fora do docroot publico.
